import { render } from '@testing-library/react';

import SearchTicketsPage from './SearchTickets.page';

// здесь нужно написать нормальный тест
test('renders SearchTickets component', () => {
  render(<SearchTicketsPage />);
});
